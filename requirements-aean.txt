# Core runtime for the AEAN organism (apodex.aean).
pydantic>=2.0

# Optional: live web dashboard (uvicorn apodex.aean.dashboard.app:app).
fastapi>=0.110
uvicorn>=0.27

# Optional: real LLM providers for ADE narratives / AVIE prompts.
# The organism runs fully offline without these (deterministic simulation).
# openai>=1.50
# anthropic>=0.34
